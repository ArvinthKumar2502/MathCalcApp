<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathcalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MathCalculator">
        <activity android:name=".outputMatrix"></activity>
        <activity
            android:name=".NumberSystemActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".MatrixActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".TrigonometryActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ElectricalActivity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".LogarithmActivity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>