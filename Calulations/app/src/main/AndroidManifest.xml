<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.calulations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Calulations">
        <activity android:name=".set_ops_activity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".Matrix_operations_activity"
            android:parentActivityName=".MainActivity"></activity>
        <activity
            android:name=".figure_words_activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".date_difference_activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".hcf_lcm_activity"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>